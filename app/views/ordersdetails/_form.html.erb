<%= form_with(model: ordersdetail) do |form| %>
  <% if ordersdetail.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(ordersdetail.errors.count, "error") %> prohibited this ordersdetail from being saved:</h2>

      <ul>
        <% ordersdetail.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :order_id, style: "display: block" %>
    <%= form.text_field :order_id %>
  </div>

  <div>
    <%= form.label :book_id, style: "display: block" %>
    <%= form.text_field :book_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
